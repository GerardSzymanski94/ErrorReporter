{
    "name": "vendor/error-reporter",
    "description": "Laravel package: wysyłka logów na e‑mail (error+) z throttlingiem",
    "type": "library",
    "license": "MIT",
    "autoload": {
        "psr-4": {
            "Vendor\\ErrorReporter\\": "src/"
        }
    },
    "extra": {
        "laravel": {
            "providers": [
                "Vendor\\ErrorReporter\\ErrorReporterServiceProvider"
            ]
        }
    },
    "require": {
        "php": ">=8.1",
        "illuminate/log": ">=10.0",
        "illuminate/mail": ">=10.0"
    }
}
